[package]
name = "bg-input-chain"
version = "0.1.8"
edition = "2021"
license = "MIT"
description = "Input action chain recording and replay for bg-driver-rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
